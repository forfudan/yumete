# 變更記錄 · Changelog

寫給用它寫東西的人看的，不是給提交記錄看的：每一條說的是**你能做什麼**，
以及**什麼變了**。編號對應 `docs/development.md` §5.2 的功能表。

Written for people who use it to write, not for the commit log: each line says
what you can *do*, and what changed under you. The numbers are the feature table
in `docs/development.md` §5.2.

## 0.1.0 · 2026-09-15

頭一版。底下幾節裏寫着「變了」的，說的是**做的過程中變的**——前面沒有上一版
可比。之所以留着，是因為它們多半在說「為什麼是現在這樣而不是你以為的那樣」，
而那正是一個從 Helix 過來的人會問的。

The first release. The sections below that say 「變了」 mean *changed while it
was being made* — there is no previous version to have changed from. They are
kept because most of them answer 「why is it this way and not the way you
expected」, which is the question somebody arriving from Helix will have.

### 命令變了 · Commands that changed

- **命令表摺成了一棵樹：41 個名字，從前是 61 個**（#283）。同一件事的幾個名字從前
  各佔一格，`:wrap` `:dense` `:bands` `:sentence` `:hanging` `:numbers`
  `:typewriter` `:focus` `:meter` `:hud` `:preview` 全是版面，卻在命令列表裏散着
  排。現在它們歸到七個父親底下：

  | 父親 | 底下有 |
  | --- | --- |
  | `:view` | `wrap` `dense` `bands` `sentence` `hanging` `numbers` `typewriter` `focus` `meter` `punct` `hud` `preview` |
  | `:check` | 原有的幾項 ＋ `merge` |
  | `:table` | 原有的幾項 ＋ `jump` `find` |
  | `:count` | `progress` `target`（不帶詞還是數字數） |
  | `:write` | `all` `as` |
  | `:buffer` | `list` `next` `previous` `close` |
  | `:theme` | 原有的調色盤 ＋ `system` `dark` `light`（`:appearance` 改叫 `:theme`） |

  **葉子的名字不變**，所以 `:dense` 現在寫作 `:view-dense`——多打一個詞，換來的是
  補全列表短了三分之一，而且看得出哪幾件事是同一類。七個名字順帶改了，因為歸了類
  之後舊名字不再說得清自己是什麼：

  | 從前 | 現在 |
  | --- | --- |
  | `:note` | `:view-punct` |
  | `:conflicts` | `:check-merge` |
  | `:row` | `:table-jump` |
  | `:search` | `:table-find` |
  | `:bclose` | `:buffer-close` |
  | `:wa` | `:write-all` |
  | `:saveas` | `:write-as` |

  **打了舊名字不會只挨一句「沒有這條命令」**：`:dense` 答的是「沒有 `:dense`，
  你要找的是 `:view-dense`」，`:prog` 答 `:count-progress`，`:punct` 兩處都有就
  兩處都說。

### 補全面板 · The completion panel

- **名字跟在代號後面，灰的**（#291）。`:yume-scheme` 列的是前端找到的方案，而 yume 裝進去
  的方案是按槽位取名的——`custom.6947b838`、`custom.cc2d1290`，一列十六進制看不出誰是誰。
  現在每一行後面跟着它自己的名字：

  ```
  scheme custom.6947b838   冰雪清韻
  scheme custom.cc2d1290   天碼
  scheme lingming          靈明
  ```

  **代號仍然是打進去的那個**（名字會變、會撞，槽位不會），名字只管讓你認出是哪一個。
  **而且搜得到**：打 `:yume-scheme ` 之後點一下 Shift 切中文，打「冰雪」就只剩那一條——
  「冰雪」既不是 `custom.6947b838` 的前綴，也根本不在裏面。

  只有「名字本身是代號」的那種行才有備注。`on`／`off` 旁邊再寫一次「on」是噪音，而且高亮
  那一行的說明本來就在底下那條。欄位窄到放不下的時候**先丟備注**——它是解釋，不是你要打
  的東西。

### 輸入法 · The input method

- **輸入法有三個狀態了，`:yume on|abc|off` 進，輕點 Shift 切中英**（#290）。從前只有兩
  個，而且 `:yume off`、`C-Space`、輕點 Shift **三個入口做的是同一件事**。現在：

  | 狀態 | 怎麼進去 | 底下那行 |
  | --- | --- | --- |
  | 中文 | `:yume on`／輕點 Shift（從 ABC） | `[靈明]` |
  | ABC | 輕點 Shift／`:yume abc` | `[ABC]` |
  | 關 | `:yume off` | 什麼都不寫 |

  「ABC」和「關」在鍵盤上看着一樣，**差別是 yume 還在不在**——而這個差別是實的：報得出
  「單獨按下的那一個 Shift」的終端開關，同時會讓**系統輸入法組不了字**（#271 就是這麼來
  的）。所以那個開關現在**只在 yume 接管着鍵盤的時候推上去**，一交還就撤掉。兩件事都
  要，而它們要的是同一個開關。

- **輕點 Shift 從 09-05 起就不靈了，到 09-08 纔發現。**（#290）修 #271 的時候撤掉的正是
  「上報裸修飾鍵」那一個開關——協議裏只有它管這件事。單測是綠的：`ShiftTap` 一直沒錯，
  錯在**終端從來沒被要求送那個事件**。

- **`C-Space` 不再是輸入法的開關，也沒有別的鍵接手。** macOS 把 `C-Space` 花掉兩次
  （Spotlight、切換輸入源），在那臺機器上它根本到不了終端；接手的 `Shift+空格` 同一天
  又退了回來，因為它在多數輸入法裏是全／半角，而 yume 「關」着的時候鍵盤正在系統那個
  輸入法手上——最要緊的那個方向（關→開）恰恰按不動。「空格快捷鍵太寶貴了…
  我建議還是做成 command。」外面那個開關一天按不了一次，值不起一個鍵；裏面那個（輕點
  Shift）本來就不佔鍵位。

### 鍵位變了 · Keys that changed

- **小寫刪，大寫剪：`d` 不再吃掉剪貼板。** Helix 的 `d` 會順手把删掉的東西存進
  寄存器，而寄存器只有一個——複製一段，路上删掉一個礙事的頓號，那一段就沒了。
  現在四個鍵一條規矩：**`d` 删、`D` 剪切、`c` 改、`C` 剪切後改**，只有大寫的那兩個
  碰剪貼板。`D` 和 `C` 在 Helix 裏本來就空着，所以沒有擠掉任何既有的鍵；
  `A-d`／`A-c`（Helix 那對「删而不動寄存器」）**退場**，換完之後它們就是 `d`／`c`
  的長寫法。表格裏也一樣：`d` 清空這一格、`D` 連帶放進寄存器——填表正是這條規矩
  最值錢的地方。

- **竪排的 `j`／`k` 走得過段尾了。** 從前 `j` 走到一段的最後一個字就停住，下一縱
  要靠别的鍵纔過得去。横排的 `h`／`l` 早就跨得過行尾（「一行不是一堵牆」），
  竪排那一對卻還在用「不越出本行」的那個函數。哪一對沿着字走，哪一對就跨得過去。

- **表格三種看法是 `t o` `t b` `t f`，詳情欄還是 `t i`。** 表格有三種看法了
  （#275、#283）：`t o` 源碼、`t b` 基本、`t f` 全。中間那一種曾經借走 `i` 一天，
  詳情欄一度改叫 `t I`；借的還了，**詳情欄叫 `t i`**（information），從前是 `空格 d`。
  `t t` 是把整個窗口讓給表格，`t q` 把窗口還回去。

- **`t` 整個給了表格。** 源碼模式下 `t` 本來是「走到下一個某字母的前面」——
  漢字沒有字母，這個鍵在中文稿子上等於空着。現在它在哪個模式下都是表格：
  `t t` 進表格，`t q` 出來，`t f` 對齊，`t ]` `t [` 走到下一張／上一張表。
  `f` `F` 照舊，till（`t` `T` 的那一半）**退場**，不留別名。

- **`Enter` 和 `*` 在 Normal 模式下退場。** `Enter` 太容易誤按——在 Normal 裏多按
  一下，頁面就跳走了。它們做的事現在是：
  - **`g/`** 找選中的那段（或光標下的詞）在整份稿子裏的下一處，跳過去；
  - **`g?`** 同一個問題，答案畫在另一個工作區，你不動；
  - **`t/`** `t?` 表格裏一欄一欄地找，同一對意思。
- **數字和動作的位置定下來了**：序列裏是「命令＋選擇＋動作」——`g3d`、`g2-5d`、
  `g30g`、`t20,20g`、`t1a2d8as`、`t2-10?`；單鍵前面的數字還是次數（`3w`、`5j`）。
- **排幾欄是 `t1a2d8as`。** `a`／`d` 各記下一欄（順排／倒排），最後那個 `s` 纔動手，
  所以打到一半不會自己排起來——`t1s2S8s` 那種寫法做不到，因為 `t1s` 本身就是一條完整
  的命令。一欄的 `t1s`／`t1S` 照舊。
- **`30G`**（或 `g30g`）去第 30 行；`G` 去最後一行。
- **`3gd`／`g3d`** 在表格裏改成「在第 3 欄裏找」，`g2-5d` 是第 2–5 欄。

### 新的 · New

- **分詞著色多了兩種畫法，而且換了出廠值。** 從前只有底色（`tint`）和字色（`ink`）。
  字色隔一個詞把墨調暗一階，問題是**暗的那幾個詞讀起來像強調**——量出來兩支墨差
  1.38:1。現在出廠是**色相**（`:word-show 色相`／`color`）：隔一個詞換成一支淡藍，
  **亮度幾乎不動**（深色頁 1.007:1，淺色頁 1.047:1），分得出來卻沒有一個詞比鄰居響。
  用的是藍↔黃這個軸而不是紅↔綠（紅綠色盲約佔男性 8%，而這個記號的全部價值就是
  分得出來）；已經上了色的段落——標題的金、連結的藍——**一點不碰**。
  ⚠️ 淺色頁那支藍比深色頁的濃：墨越暗，眼睛分辨色相的本事越差。

  第四種是**線**（`:word-show 線`／`line`）：字的墨、紙的色一點不動，只在詞底下
  畫一道淡線。横排是整個詞下面一條、詞與詞之間斷開；**竪排不提供**——竪排一格只有
  一條下横線，而 Insert 的光標就是那一條。

- **`yumete --lang=en`，以及 `:language`（短名 `:lang`）。** 編輯器自己說話的語言
  從前只能改配置再重開。`zh` 繁體／`zhs` 简体／`en` English，命令當場換，**回話已經
  是新語言**——一個讀不懂繁體的人打了 `:language en`，如果確認那句仍是繁體，他就
  不知道成沒成。

- **搜索認大小寫的方式變了：模式裏沒有大寫就不分**（#301）。從前 `/todo` 找不到
  `TODO`，只有 `:s` 有一個手寫的 `i` 旗標。現在是 smart case，和 Helix 一樣：
  沒有大寫就不分大小寫，**打出一個大寫就是在說「這個要照字面找」**。稿子是漢字，
  漢字沒有大小寫，所以絕大多數搜索白拿這一條；而 `TODO`、`ISBN`、人名這種要照字面
  找的，本來就帶着大寫。一次性反過來寫 `(?-i)`，一直反過來寫
  `[editor] smart_case = false`。

- **光標旁邊那個牌子出廠是 `full` 了**（#284）。從前出廠是 `basic`——一顆藥丸，一個字
  都不蓋，自己去找頁面上的空白，理由是「出廠那一檔在最壞情況下要和 `off` 一樣不損失
  東西」。現在是 `full`：一塊帶框的面板，釘在光標正下方，蓋住那裏的正文。**牌子先得
  一眼看得見**，而被蓋住的那幾個字，光標挪一下就露出來。不想被蓋，`:view-hud basic`
  一句話回去。

- **一存之下檔案翻了幾倍，`:w` 先問一句**（#295）。一個鍵可以把一份稿子變成三份那麼大：
  `t F` 對齊一張表，最寬那一格是一整段，於是每一行都補到那麼寬——這份 `development.md`
  就一鍵從 425,694 字節變成過 2,945,642（#292，那一次已經修了）。修掉的是那一次；稿子和
  磁盤之間最後一道門是存檔。現在 `:w` 遇到**又翻倍、又多出 256 KB 以上**的時候停下來，
  在螢幕中央開一扇「安全核驗」：正文說的是數字——「這一存會把《第一章.md》從 25 KB 變成
  1.1 MB，44.1 倍」——不是「幅度較大」，因為形容詞正好是你沒法覆核的那一半。三個答案：
  `y` 照存、`d` 看改了什麼（就是 `:diff`，**這一存不做**）、`n` 不存（`Esc` 同義）。
  **兩個條件缺一不可**：只看翻倍，一份 3 KB 草稿寫成 7 KB 就會被攔，那是一個早上的活；
  只看多 256 KB，一本長篇加一章就會被攔。新檔案第一次存**永遠不問**。問題開着的時候
  鍵盤全歸它，不是那三個鍵的一個都漏不到底下的稿子上。今天只裝在 `:write` 上。

- **你自己裝進 yume 的方案，`:yume-scheme` 裏也有了**（#169 的另一半）。yume 重構之後方案
  分兩處放：出廠那幾個是 `schemes/<名字>.toml`，使用者自己裝的各佔一個槽位
  （`…/Yume/installed/<八位十六進制>/`，裏面是 `custom.ytab` 碼表、`custom.yzg` 字根、
  `custom.ycdv` 拆分、`custom.yscm` 設置）。從前只掃前面那一半，於是裝好的冰雪清韻與 天碼**一個都列不出來**。現在兩處都掃：名字取自 `custom.yscm`，代號是 `custom.<八位>`，
  碼長與選重鍵照那份設置辦（不猜），拆分注解走那個方案自己的 `custom.ycdv`。實測：從前 5 個方案，現在 7 個——`custom.6947b838 冰雪清韻`、`custom.cc2d1290 天碼`，打 `shu`
  各自出「似乎」「酸」，注解畫得出「西一厶夊 · Sx-Hv-Uv-Dv」。Windows 的 `data/custom/` 與
  方案管理之前的裸 `custom/` 也認，同一格只算一次。

- **`t a`：全窗表格裏，太寬的格子折到自己那一欄的下面幾行**（#283）。2026-09-07：「把所有超长的单元格都在单元格下方的空行中 soft wrap。」一個字都不藏、也
  不用左右捲：一行有多高看它最高的那一格要幾行，行號只畫在起頭那一行，下面幾行是同一
  行的續行。`t w` 和 `t a` 是同一件事的兩個開關，**永遠不會同時亮**，而且**兩個都是離開，
  不是換一種藏法**（2026-09-08）：在摺起裏按 `t a` 換成折行，在折行裏按 `t w`
  是**全部攤開**，各自再按一次也回到「攤平」。要從折行換到摺起，攤平了再按 `t w`。
  （沒有做成 `t w` 三檔循環：那會讓同一個鍵在正文裏是開關、在窗裏是循環。）正文頁面上按 `t a` 會說要先
  `t t`——那裏的行是共用的折行層算的，它沒有懸垂縮進——**設定照樣記下**。

- **全窗表格是一扇窗，走不出去了**（#283）。`gg`、`G`、`g30g`、`:120`、搜索在窗裏一律
  夾在表內並說一句，滾輪滾到頭就停；出去的路只有 `t q`（交回窗口）、`t o`／`t b`／`t f`
  （換一種看法）和 `t ]`／`t [`（下一張表）。從前它是漏的：`G` 一按光標跳進底下那一章，
  窗裏沒有表可畫，窗口就**自己**交回給正文——全窗表格會在沒人要求的時候結束。堵上之後
  窗裏的行號改成**這張表自己的條號**（第 1 條、第 2 條…），狀態欄的「第 N 行」和 `t1g`、
  `t20,20g` 數的都是它：窗裏每一個數字都指同一件事。

- **`:view-hud off｜basic｜full`：光標旁邊那個牌子，三檔**（#284）。打了一半的命令在光標旁
  邊有一份，一直是一根細線連過去。現在它是一顆**藥丸**（默認 `basic`：金字暗底，角指
  回光標，**一個字都不蓋**——自己去找頁面上最近的空白），要更醒目就 `:view-hud full`：一塊
  **帶框的面板**釘在光標正下方，位置固定，**蓋住那裏的正文**。要清淨就 `:view-hud off`，狀
  態欄右端那一份還在。
  外框和「蓋字」是同一個決定：帶框的面板要的是光標附近一塊三行高的空地，一頁稿子上沒
  有這種東西，所以要框就得蓋；而字一旦畫在跟正文同一張紙上，沒有框就沒有東西說明哪幾
  個字不是你寫的。默認不是 `full`，因爲 Normal 之下這個牌子寫的正是**你打的那個數**，
  釘在光標下面蓋掉的正是 `3`、`2t`、`d3l` 要數的那幾個字。
  `full` 會避開 `空格` 選單、`:` 選單、挑選器和表格側欄；候選框比它大，兩個都要光標下
  面那一格的時候，候選框贏。`:render` 不寫這一項——那四項說的是「檔案怎麼畫」，這一項
  說的是「編輯器怎麼跟你說話」。

- **`gx`：跟着連結走**（#285）。光標下面那個 `[文字](目標)` 或 `[[第三章]]`——**Ctrl
  點一下**也一樣。網址交給系統的瀏覽器；本書的另一篇**在這個窗口裏打開**，成一個緩衝
  區；`#雪` 這種就跳到那個標題（先開那一篇，再跳）。光標在連結的哪一段都算數，包括
  所見即所得 藏起來的那個目標——看不見的那一截，不能要求讀者站上去。
  `[[第三章]]` 是**篇名**不是檔名，後綴是這本書的事；相對路徑照連結所在的那個目錄算。
  **只有 `http` 和 `https` 交給系統**：稿子是會從郵件裏來的檔案，而 `open` 會啓動任何
  一個聲稱認得某個協議的程序，所以 `mailto:` 和別的協議一律說出來、不執行，絕對路徑
  （`/etc/…`）同樣不跟。交給系統的那一步不經過 shell——網址是作爲一個參數遞過去的。


- **`t w`：太寬的格摺起來，`t i`：摺起來的那一格在詳情欄裏讀全**（#283）。稿子裏的表
  格常有一欄是一整段話，它一寬，後面每一欄都被擠出屏幕外——`t w` 把超過 32 格的那一
  截收起來，斷口畫一個 `>`。**用的是半角 `>` 不是 `…`**：Unicode 給的每一種省略號都
  是東亞「模糊寬度」，編輯器算一格終端算兩格，那一行往後每一欄就差一格，而且偏偏差
  在摺起來的那幾行上。光標所在的那一格永遠不摺，所以摺着照樣改字。**只在 `t f`（全）
  底下摺**——「基本」那一級的規矩是不藏、不摺、不替換，按了它會直說要 `t f`，不會自
  己把級別調上去。要讀全那一格就開 `t i`（詳情欄）。

- **詳情欄在 markdown 表格裏也開得起來了**（#283）。從前 `t i` 問的是
  「這個**檔案**是不是一張表」，於是 `.md` 裏的每一次都被當成腳注問題轉走，一欄一欄
  列出來的那個面板只有開 `.csv` 纔見得到。現在問的是「光標**站在**哪裏」。順帶：一行
  的欄位是按表格自己的分欄算的（從前拿 `|` 硬切，兩頭各多一個空格子，於是面板整體答
  偏一欄），而且**一份文件裏的第二張表按它自己的欄名算**——從前拿的是第一張表的欄
  名，`t y` 在第二張表裏報的是第一張表的欄，而且報得理直氣壯。

- **`:convert`：簡繁，交給 opencc**（#241）。`:convert s t` 整份轉繁，`:convert t s`
  轉回來，不帶參數就列出七個代號和每一個能去哪裏。**這件事 yumete 自己不做**：发 在
  头发 裏是 髮、在 发现 裏是 發，一張字表答不了，要詞典加分詞，而 OpenCC 已經認真做
  了十五年——自己再寫一個只會寫出一個更差的，而稿子要信它。沒裝就說一句怎麼裝，一個
  字都不動；macOS 上 `:convert-opencc install` 直接替你裝，別的系統只把命令印出來
  （那要 sudo，全屏編輯器不是該問密碼的地方）。opencc 自己的五個之外多兩個：**`c`
  大陸通規繁體**和 **`g` 古籍通規繁體**——OpenCC 的繁體是港臺字形（爲 説 裏），大陸
  出版社排繁體書用的是通規（説 内 吴），差的是字形不是用詞，幾十個字一個換一個，所
  以它是一張表（抄自 GujiCC）。從 `c` 出發就把那張表倒着走一遍回到 opencc 認得的字
  形再交給它，`:convert c tw` 是這麼走的；通規把 蝨 併進虱、嶽 併進岳的那幾個字倒不
  回去，原樣留着不猜。帶 `p` 的那幾個配置連詞彙一起換（内存 → 記憶體），會改字數也
  回不去，所以要明說 `force`。**反悔按 `u`**，一次轉換是一步撤銷——別拿反方向當撤
  銷，繞一圈回來的不是你原來那份稿子。

- **`:check-charset`：排版社沒有的那個字**（#240）。找出不在**任何**字集裏的字——通
  用規範、臺灣、香港、古籍四個都沒有的那些。它可怕在不在你這邊出錯：你的字體有它，
  屏幕上一切正常，排版社的沒有，三個星期後它變成一個豆腐塊，而且每一次印刷都這樣。
  資料早就在編輯器裏了——`:yume-scheme` 載的那份拆分表帶着每個字的字集。**一個字一
  行，不是一處一行**：一個帶生僻字的人名出現四百次是**一個**決定，四百行只會把另外
  那三個字埋掉，所以給的是第一次出現的地方、全文多少處，加上它的 Unicode 區塊——區
  塊纔是「字體會不會有它」的答案。

- **`:check-punct`：那個沒有收尾的「**（#238）。三樣東西：中文裏的半角 `,` `.`
  `!` `?`（輸入法晚切一下就有一個）、該是 `……` 的 `...`、以及**開了口沒收尾的
  「」『』（）《》〈〉【】**。第三樣纔是它存在的理由——少一個 `」`，它那一行看不出
  任何問題，而從那裏往後整章的引號意思全反了，校對的人盯着出事那一頁什麼也看不出
  來，因為那一頁是對的。只在**中文旁邊**纔算數：`3.14`、`1,000`、`README.md`、
  整句英文裏的半角標點全都對，`` `代碼` `` 和圍起來的整塊一概不看。跨段的引文也不
  會被冤枉：每段重開一個「、最後一段纔收的排法是對的。

- **`:view-sentence`：一句一縱地校對，文件不動**（#237）。手冊從前教的是
  `:%s/。/。\n/g`——拿改文件當看法用，撤銷少按一次就寫進去了。現在竪排開
  `:view-sentence`，每個句號、問號、嘆號、省略號之後就另起一縱，`:view-sentence off` 回去，
  稿子一個字節都沒動。長句照常換行（只是下一句一定另起），句末的引號跟着句號走：
  `「你回來了。」` 是一句。斷的就是 `(` `)` 跳的那條界——縱頭正是按 `)` 會停的地方。

- **着重號：竪排的 `*強調*` 點在字旁邊**（#236）。漢字不會斜——中文的強調從來是每個字
  旁邊點一個點，竪排點在右邊。所以竪排時 `*這樣*` 直接畫成着重號，不用新語法：`*字*`
  本來就是 `<em>`，而 `<em>` 在中文裏的樣子**就是**着重號。點畫在注音／旁置句讀／稿紙
  刻度那一欄，排最後——那一格有注音或者掛着標點就歸它們。那一格是排版時**買下來的**，
  和注音一樣按需要留，所以密排也點得出來，沒有強調的段落一格都不多花。`**粗**` 不點：
  那是字重，兩樣都點半頁都是點。

- **`:diff`：改了什麼，按詞比**（#235）。中文的一段就是一行，所以所有按行比的工具在
  中文稿子上都等於沒說——挪一個「的」，`git diff` 把整段抹掉再畫回來。這一條按**詞**
  比，用的是編輯器自己的分詞：`ch01.txt:2: 那年冬天，雪下得[-很早-]{+極早+}。`，
  `gf` 跳過去。不帶參數是跟磁盤上那一份比（「這一坐下來我改了什麼」），帶路徑就是跟
  昨天那一稿比。

- **`:ruby-auto`：整份稿子按詞注音**（#234）。一個字一個字注是注不完的。這一條把選中
  的那一段——沒有選區就是整份——按詞走一遍，讀音從宇夢的讀音表按**整個詞**查：了 在
  為了 裏是 `le`，在 了解 裏是 `liǎo`，一個字一個字查的工具必然錯一半（Word 的「拼音
  指南」就是這麼錯的）。已經注過的原樣留着，你手改過的讀音不會被蓋掉；注了幾個詞狀態
  欄會說，不要就 `u`，一步全退。同一個拼法只差聲調的（為 `wéi`／`wèi`、難、好、教、中）
  讀音表本身不帶調，分不出來，取常用的那一個。真正好用的是 **`:ruby-auto rare`**：每
  個字都有讀音的小說是課本，只在真正生僻的那幾個字上有讀音的小說纔讀得完，所以它只注
  **四套現行字集（大陸通規、通規繁、臺灣、香港）都沒收**的字。寫出來的是這份檔案本來的那種方言（`.typ` 出 Typst，別的出 HTML），逐字一組，竪
  排那一欄早就替它讓好了位置。

- **`:check-usage`：一本書裏兩個寫法**（#233）。「裏」四百次、「裡」三次，這不是三個
  錯字——兩個都是規範的漢字——這是一份還沒定下來的稿子，而沒有任何工具會說話：Word 查
  的是病句，Grammarly 是英文的，拼寫檢查器按空格切詞，一整章在它眼裏是一個詞。
  所以這一條**不問字典，問這份稿子**：同一組的兩個寫法都在這裏寫過才算數，寫得多的
  那個就是它想要的那個。結果是一個 `檔名:行號:` 的緩衝區，`gf` 跳過去改。自帶六十來
  組（裏/裡、為/爲、臺/台、著/着、甚麼/什麼、身分/身份……）；這本書自己的名字寫在
  `[editor] usage_groups`，一行一組——阿嬌和阿姣任何異體字表都不會有，而它恰恰是一年
  下來最會走樣的那一個。

- **從 Excel 直接 ⌘V 貼進表格**（#226）。Excel、Numbers、LibreOffice、瀏覽器裏的表格
  往剪貼板上放的都是製表符分隔的行，而終端把它整批送進來的那一條路從前是被拒的——
  格子裏不能有 Tab，所以一個表格編輯器最該收下的那一種粘貼，恰恰是它唯一不收的。
  現在它和 `p`、`空格 p` 走同一條路：從光標所在的那一格往右往下填，`|` 表格不夠寬就
  自己長欄，CSV 貼不下就直說。順手修掉一個原本就在的錯：貼到表尾之後的新行從前寫在
  檔案最末（空行的後面），兩行貼進「字,說明 / 木,樹」會變成「甲,乙 / 丙 / ,」。

- **表格有三種看法，`t` 那一組鍵切換**（#275）。整頁的表格視圖不是另一種東西，它只是
  一張表剛好從檔案的頭鋪到尾——所以稿子中間的一張表也能有同一件事：

  - **正文**（`t q`）——`|` 就是一個字符，鍵是正文的鍵。
  - **表格操作**（`t i`）——逗號、跳格、`|` 一個都不藏，但 `Tab`、`t o`、`t s`、`gd`
    全是格子的意思，而且**表格那幾行不再折行**：一行就是一行，數得清第幾欄。
  - **真表格顯示**（`t t`）——畫成格線，也就是 `.csv` 一開就是的那個樣子。

  三種之間直接切，`t i` 在真表格顯示裏是回到中間那一種，不是回正文。

  管到哪幾行，看檔案自己說了什麼：`.csv` `.tsv` `.md`、或者旁邊有 schema 的，是**整個
  檔案**的狀態——在兩張表中間的段落上按也算數，每一張表一起變，光標走開了也還是表格；
  `.txt`、`.yaml`、貼進稿子的一段跳格分欄，只管**光標底下這一段**，走出去就回正文。
  `|` 表格自己每一行都寫明了它是什麼，所以它走第一條，哪怕坐在一份 `.txt` 裏；猜出來
  的那一段纔走第二條——猜的東西不該在讀者走開之後還留在畫面上。

- **`::`：忘了命令叫什麼，就說它是做什麼的**（#224）。空的 `:` 行上再按一次 `:`，
  那一行就從「命令叫什麼」變成「命令做什麼」：`::竖排` 找到 `:layout vertical`，
  `::排序` 找到 `:table-sort`，`::lyt` 找到 `:layout`。

  這一行**開着輸入法**（單按 Shift 切中文，跟 `/` 一樣），因為它本來就是給中文問句用
  的。說明本來就寫了繁、簡、英三份，**三份一起找，只顯示你在用的那一份**——繁體版上
  打「竖排」也找得到。打不全也行：字母順序對上就算（`tbsort` → `table sort`），手滑
  成 `laoyut` 也還在。

  **`⇥` 不執行任何東西**，它把整條命令寫回 `:` 那一行，你自己按 Enter。所以 Enter 跑
  的永遠是你看得見的那一行，不是它猜的東西——`:q!` 是不能撤銷的。`↑` `↓` 挑，退格退空
  了回 `:`，`Esc` 回正文。

  每條命令還可以在 `messages.toml` 裏寫一行 `find = "直排 縱書 tategaki columns"`：
  搜得到，但不顯示。說明可以寫得短，而「同一件事的另外四種說法」照樣找得着。

- **文中的一段也可以是表格**（#216）。整個檔案不是表，裏面那幾行是：一張碼表貼進了章
  節，`dict.yaml` 的正文在 `---` 前言底下，LaTeX 的 `tabular`。把光標放進那幾行按
  `:table`，它就是格子；出來的時候檔案一個字節都沒動過。

  站在哪個分隔符上就按哪個分（一行裏跳格和逗號都有的時候，這就是你說「這一個」的辦
  法）；選中幾行也行；都沒說就按跳格、逗號、分號、`&` 的順序試。往上下走到不含那個分
  隔符的行為止——空行是一個界，`## 第三章` 也是一個界。進去之前先對一遍欄數，對不齊就
  不進去：寧可說不，也不要在別人的稿子上畫一張歪的格子。

  **它只讀不改寫**：走格、`t g`、`t /` `t ?`、`t y` `t p` 照常（`t p` 只寫到區塊最後
  一行），會重排整行的那些一律不做。真要整理它，`:table-pipe` 或 `:table-csv` 把它變
  成表格。

- **版本號說得出「我跑的是哪一份」。** `yumete --version` 現在答
  `0.1.0-dev.20260905110744+bbc1485.dirty`——版本、編的時間、編自哪個提交，以及那時候
  工作區有沒有沒提交的改動。同一行也印在每一張圖的最下面（`--shot`、`--html`、`:shot`
  都是），畫在頁面外面，所以一張貼進報告的圖自己說得出它是哪一版畫的。

- **編輯器說的每一句話都可以自己改了**（#151）。`crates/yumete-core/messages.toml`
  一條消息一段：上面一行 `#` 說明它在什麼時候、什麼條件下出現，`key` 是那個條件的
  英文名（`readonly.refused`、`reload.refused-dirty`），下面 `zht` `zhs` `en` 三行
  是話本身。**改哪一行都立刻生效，不必動代碼。**

  從前中文句子本身就是鍵，所以在這個檔裏改中文是**沒有用的**——螢幕讀的是代碼裏那
  一份；而每改一個字，配的英文就悄悄失效。現在會變的是話，不變的是鍵。

  `[editor] language` 也多了一檔：`"zh"`（繁體，預設）、`"zhs"`（简体）、`"en"`。
  简体那一份是機器轉的，等着一條一條讀過去——简体不只是換字，有時候是換詞。

- **`:readonly`——鎖住一份不許改**（#213）。`:readonly on` 之後這一份就是只讀的，
  狀態欄上掛着 `[只讀]`；Insert 模式連進都不進去，`d`、`o`、`p` 一概不動它，`u` 也不
  行——往回也是動。`:readonly off` 解開。`yumete -R 檔案`（`--readonly`）鎖住**整個
  這一次**，包括後來 `:open` 打開的。

  **磁盤說只讀的檔案，現在開的時候就知道了。** 以前 `chmod 444` 的檔案照樣讓你打，
  一直打到 `:w` 纔說不行——那時候一下午已經過去了。

- **`:reload`——檔案在外面被改了，把它讀回來**（#214）。`:reload` 重讀；你這裏有沒存
  的改動它會攔下來，`:reload!` 纔是「丟掉我的」。`:reload-auto on` 讓它自己讀：檔案
  在外面變了、而你這裏乾淨，就直接讀進來；**你這裏有改動的時候它不動手**，只提醒一
  次——合併不是編輯器該替你做的決定。

  `:e!` 和 `:o!` **退場**，不留別名也不留提示。`open!` 讀起來像「打開，但是更用力」，
  而它真做的事是把你這一下午扔掉。

- **候選框可以收起來了：`:yume-panel bare`**（#211）——首選**直接寫在句子裏**，光標
  停在它末尾，已打的編碼在光標下面那一行。一張候選框都不畫。要挑的時候 `Tab` 召出
  完整的候選框，管這一個詞，詞上了屏它就跟着走。

  九成的按鍵都是取首選，為了它蓋掉九行稿子並不划算——而宇夢自己的輸入法面板已經有一
  張完整的候選框了，寫小說的這張紙上不需要第二張。畫進正文的那幾個字**不在檔案裏**，
  但它們**在頁面上**：光標、`j`、滑鼠、折行、禁則算的是同一頁，所以後面的字不會被擠
  到看不見的地方去。配置檔是 `[panel] display`；`/` 搜索照舊有候選框——它在狀態欄上
  組字，那裏沒有正文可以畫。

- **每一張 `|` 表格，在頁面上都是齊的**（#212）——不只是光標所在的那一張，也**不動檔案**。
  `t f` 把表格排齊、寫進原文，那是給 GitHub 和別的編輯器看的，對；可是所見即所得把
  `` ` `` 和 `**` 藏起來之後，每一行少掉的格數都不一樣，**同一份原文不可能在兩邊同時是
  齊的**。所以齊不齊這件事改在頁面上算：每一幀量一次，補出來的空格是虛擬文字（#210），
  一個字節都不會進到檔案裏。

  量的是兩根 `|` 之間**整格的寬度**（不是去掉空格之後的內容），所以你自己敲的空格照舊
  算數；每一行都對欄寬有一票，`|---|` 那一行也算——虛擬文字只能加，不能減。`---` 是畫
  出來的，補的是 `-`；`:---:` 的冒號一個都不會被蓋掉。`:render off` 之下不補——那時候
  頁面就是檔案；直排也不補。

- **Windows 上能用了**（#219）——本來也能編出 `.exe`，但那個 `.exe` 上有四件事是錯的，
  而且編譯器一個字都不會說：設定與資料落在**跟着工作目錄跑**的相對路徑上、
  「寫入按身份認」在沒有 inode 的地方悄悄失效、`:sh` 和 `:!` 去找 `/bin/sh`、
  一個字幾格問不出來（於是 `——` 一行差十格）。現在：
  - 設定和資料都在 `%APPDATA%\yumete`——**同一個目錄**，`config.toml` 在根上，
    `data\` 和 `schemes\` 在旁邊，跟宇浩自己的 `%APPDATA%\Yume\` 一個形狀。
    `XDG_CONFIG_HOME`／`XDG_DATA_HOME` 在**哪個平台上都仍然優先**。
  - 同一個檔案認得出來了（`GetFileInformationByHandle`：卷序號＋檔案索引）。
  - `%ComSpec%`／`cmd.exe` 加 `/C`；`~` 在配置裏和在命令行裏是同一個家。
  - `ambiguous_width = "auto"` 那邊走的是控制台 API 而不是回覆，比 Unix 那條還準。
  - **會去找宇浩自己裝的碼表了**——`%APPDATA%\Yume\data\compiled` 那一套，
    覆蓋層在前。已經在打卿雲的機器不必再編一次。碼表攤平在一個資料夾裏也認。
  - `[ime] data_dirs` ——資料在哪，自己說了算，哪個平台都能寫。
  - `scripts/build.sh` 就是 Windows 的構建腳本，用 Git Bash 跑。
    不另寫一份 PowerShell：那份檔案清單必須跟 `data_manifest` 一致，
    抄第二份就是第二個會忘記更新的地方。
  - 「碼表沒有裝」現在會把**該放進哪個目錄**寫出來。

- **`:yume-commit delayed|unique|fluency`**（#209）——**上屏方式**：碼打完了什麼時候
  上屏。`delayed` 延遲（頂字，出廠）、`unique` 唯一（也可以寫 `auto`）、`fluency` 整句。
  三個名字是 yume 自己的，合成也在 yume 的核心裏，所以這裏選的那一個在輸入法自己的面板
  裏是同一件事。換方案不會丟；拼音沒有碼表可以逐段查，永遠是整句，命令會直接說出來。
  配置檔是 `[ime] commit`，不寫就交給輸入法按方案定。
  **`:yume c` 現在不明確了**——`chaifen` 和 `commit` 都以 `c` 起頭，寫 `ch` 或 `co`。
- **`:word`**（#202）——`words` 和 `segment`本來是同一個問題的兩個名字：
  詞在哪裏斷。現在一條命令：`:word segment on|off`、`:word-show`、
  `:word-list reload|edit|global`、`:word-level less|more|full`。
  `level` 兩本詞典都管得着——內置那本調門檻，宇浩的語言模型調每個詞的獎勵
  （+2.0 / 0 / −1.5 nats，量出來的）。設定裏的 `segmentation_threshold` 換成
  `word_level`（寫了舊的會被指出來，不是悄悄忽略）。
- **十套皮膚**（#203）——藍曬、琥珀、莫高、莫蘭迪、夜螢、明度階、陶窯、靛橘，
  加上原有的墨香和黑白。命令不能打中文，所以名字是英文（`ink`、`bw`、`cyan`…），
  拼音是別名（`moxiang`、`heibai`），中文名寫在註釋裏。每一套就是八個色號。
- **`--shot --html`**（#204）——同一幀，帶顏色，給要**看**主題的審查用。
- **側欄可以翻頁**（#205）——`J` `K` 一次十二行，`g` `G` 到頭到尾。
  大綱也學會了把**目錄**扔掉：底下不到三行正文的標題、和上一條重名的標題，
  都是目錄行不是章節——資治通鑑 862 → 295，紅樓夢 240 → 121。

- **`:help`**（#167）——鍵和命令，開成一份可以讀、可以搜的檔案，四節：
  `chinese`、`vertical`、`table`、`commands`。內容是編輯器自己報上來的，
  所以不會和實際的鍵位脫節。
- **`:tutor`**（#154）——一課。把課文抄成**你自己的一個檔案**，邊改邊學；
  `u` 是第一課，所以裏面每一個會破壞東西的鍵都可以放心按。
- **打了什麼看得見**（#193）——右下角（vi 的 `showcmd`）、光標旁邊的 HUD，
  以及按下 `g`、`t`、`m`、`空格` 之後彈出的鍵位面板。
- **`:view-typewriter`**（#166）——打字機模式：正在寫的那一行停在畫面中間，紙往上走。
- **`:view-dense off` 也給橫排了**（#181）——行與行之間留一行。竪排裏它是縱與縱之間。
- **`:markdown`**（#198）——`footnote`（號碼自己找空的，文末的註一併開好，
  光標停在註裏）、`footnote inline`、`table 3x4`。
- **`[language.…]` 設定**（#197）——每種檔案自己說要跑什麼：
  `:view-preview`、`:format`、`:run <名字>`。`tinymist` 不再寫死在程序裏。
  項目自己的設定也可以定，因為安全靠的是**怎麼跑**：不經過 shell，
  佔位符整個當成一個參數，寫壞了在載入時就指出是哪個檔案的哪一條。
- **表格**：表頭上面一行**欄號**（#184）、`t20-20g` **跳到某一格**（#185）、
  `:table-sort 1 a 2 d` **多欄排序**（#183，`t1a2d8as` 是鍵盤上的同一件事）、
  詳情欄**列出每一欄**並帶欄號（#186）、詳情欄可開可關可調寬、跟着光標捲動（#187）。
- **預覽伺服器是活的**（#192）——地址記得住（再按 `:view-preview` 是報地址、
  重開瀏覽器，不是重新編譯整本書）、狀態列有 `[preview]` 標記、
  上次崩潰留下的伺服器下次啟動時會被認領並停掉。

- **`yumete -n` 從空白開始**（#267）。不帶檔名的 `yumete` 照舊接着上次——那是說好的
  ——但從前沒有辦法說「這次不要」。`-n`（`--new`）跳過那一步，開一個空 buffer。

- **`t H`：第一行是欄名，還是跟別的行一樣是資料**（#217）。一張碼表沒有表頭，第一行
  就是第一條碼；而 markdown 表有表頭。從前只能猜，猜錯了整張表往上錯一行。`t H` 一按
  就換（`:table-header on`／`off` 是同一件事），欄名沒有的時候按 `1` `2` `3` 叫。
  markdown 表與文中的一段不歸它管——那兩種的表頭是分隔線說了算的。

- **markdown 表格有自己的底色了**（#270）。代碼塊、引文、`:::` 塊本來就有一層淡底，
  只有表格沒有，於是章節裏的一張表看起來像「碰巧有幾根豎線的散文」。現在同一層底色，
  一眼看得出哪幾行屬於這張表。`:render off` 之下照舊什麼都不畫。

- **`:` 命令面板有邊框和名字了**（#273）。它和快捷鍵提示面板開在同一個角上、隔一個
  按鍵，從前一個有圈一個沒有。現在同一圈同一個圓角（`[panel] rounded`），左上角寫
  「命令」；`空格 f` 那幾個選擇器同理，名字從頁腳搬到了框上。

- **`/` 一按就先寫上次找的那個**（#274）。灰色的，`Enter` 就是再找一次它，直接打字
  就是找新的。`Enter` 本來就是這個行為，只是從前不說。

- **`t e`：這張表的規格開在另一半**（#218）。整份檔案是一張表的時候，`t e`（或
  `:table-schema`）把說明它的那份 `.toml` 開在另一個工作區——沒有的話，先在資料旁邊
  的 `.yumete/tables/` 裏寫一份出來。**寫出來的那份說的就是眼下看到的樣子**：分隔符
  是它猜到的那個，`t H` 說過的「第一行是資料」也在裏面，欄名照首行。所以開了它讀回
  去，一格都不會變；接下來改的每一項，改的都是眼前看得見的東西。鍵還在表格這一半，
  `空格 w` 過去改。

- **`:table-new 3 4`：寫一張空表，直接就能打**（#276）。三行四欄——**首行是欄名**，
  下面那條 `| --- |` 是標點不是一行，所以「三行」數的是你看得見的三行。上下各留一個
  空行（`|` 行黏在段落上，Markdown 就不認它是表格了），光標停在第一個欄名裏、已經是
  編輯模式，⇥ 走下一格。不寫數字是 3×3。
  **舊的 `:markdown table 3x4` 退場，不留別名**：那個拼法是「欄 × 行」而且行不算表頭，
  跟這裏正好反着；一件事只留一個拼法。

### 這一輪修好的 · Fixed, the fourth night

- **`:diff` 現在是紅刪綠增，不再讓人數括號。** 從前那份清單寫成 wdiff 的記法
  （`[-很早-]{+極早+}`），中文裏符號密度太高，「有些難懂」。現在去掉的那一段畫成
  **紅底**、加上的畫成**綠底**，四個記號從頁面上撤下來——文件裏還是那四個記號，
  複製走、用 `/` 搜都看得到，它們只是不上屏幕。`⏎`（換行加了或少了）照畫，也帶顏色：
  看不見的變動正是最需要畫出來的那一種。

- **自動認詞裝上的詞活不過一個按鍵。** 每個掃描線程送**兩次**——答案，以及線程結束時
  補的那條空的。主迴圈一輪收一條，於是那條空的在下一個按鍵到達，把剛裝進去的兩百個詞
  全清了。`yume.md` 裏的「宇夢」掃多少遍都不著色，就是這一條。

- **淺色頁上找不到光標。** 光標是終端自己畫的，顏色來自讀者的終端配置，它不知道頁面
  翻成了淺色——深色配置下就是一個白方塊落在米色紙上。現在頁面把自己的墨和紙都告訴
  終端（OSC 10／11／12），所以光標看得見，**光標底下那個字也看得見**（終端算那個字
  用的是它自己的背景色，不是格子的）。退出時三個都還原。

- **状態欄的座標說全了，而且說的是中文。** 横排是「行 23, 列 11, 字 6」——列是半角
  格數，字是字符數，中文頁面上這兩個不一樣；竪排是「縱 23, 橫 11, 字 6」，就是同樣
  三個數轉了個身（縱＝横排的行，橫＝横排的列，用縱橫是爲了不和行列混淆）。從前橫排
  那一句是寫死的英文 `Ln 1, Col 1`，`--lang` 也換不掉。窄屏上「字」先讓位，好讓
  光標那個字的碼位讀得出來。

- **`/` 的提示行寫「搜索:」了**（`?` 是「倒搜:」，英文 `search:`／`rsearch:`），
  照 Helix 的樣子——一個孤零零的 `/` 說的話配不上它佔的那一行。

- **正文頁上的表格不再自己彈出詳情欄。** `t f` 從前會彈，而那塊面板佔掉五分之一的
  寬度，一出現，表格上下的段落**當場重排**——往下滾過一張表，整頁跳一下。現在
  `t o`／`t b`／`t f` 一律不自己開，要看按 `t i`；只有 `t t`（窗口整個交給表）還自己
  開，那裏沒有正文要顧。

- **表格狀態欄上的「· 字」去掉了。** 它和下面一行的 `T` 說的是同一件事。`T` 現在寫
  「按格移動」／「按字移動」——按下去會變成哪樣，它自己說。

- **提示行右端有了落款。** `宇夢編輯器`（英文 `Yumete`），用行號那一檔的淡色，
  擠不出兩格空隙就不畫——它讓位給提示，不是反過來。

### 再修好的 · Fixed, the third night

- **`h` `l` 過不了行尾。**（2026-09-08：「无法通过左右 h l 按键从一行末尾到另一行
  開頭。表格、正文都不行。」）一行不是一堵牆：`l` 走出一行的末尾就是下一行的開頭，`h`
  照樣走得回來，左右方向鍵（包括插入模式裏的）跟着。行內那一對還在，量一行有多長、一
  格到哪裏爲止用的是它——但**讀者按的鍵不是量尺**。表格行上 `h` `l` 照舊走格子。

- **正文裏的 `d` 刪不掉換行，說的是「格與格之間的分隔符刪不掉」。** 一份 `.md` 只要裏
  面有一張表，打開時就會替它掛上一份表格視圖，好讓那幾欄一開頁就是齊的；而「什麼不許
  剪」那一半的閘只問了「這份視圖在不在」，於是**整本書每一段的行尾**都被當成格與格之
  間。現在它問的是這一刀有沒有真的伸進格子裏——**格子只管它自己佔的那幾行**，跟鍵位那
  一半（`table_here()`）和插入那一半本來就守的是同一條法。

- **摺起來的表格，一按 `i` 整張表右移一格。**（#283）攤開那一格不撐寬自己那一欄，是靠
  告訴排版「這一行真正藏起來的是哪幾段」；可算寬度的時候**沒有把摺痕那個記號算進去**，
  而摺痕是佔一格的。於是光標那一格明明沒摺、明明在別的欄，隔壁那些摺着的行還是各多出
  一個空格——一按 `i`，牆就整片挪。現在記號是**按頁面真的畫了幾個**來算的。

### 又修好的 · Fixed, the second night

- **摺起來的表格，光標一動就卡。**（#283）2026-09-07：「不是说撑开的时候卡，而是
  不撑开的单元格也卡。」量出來是每按一鍵 **15.4 毫秒**，而且**哪一欄都一樣**——在兩個字
  寬的 `#` 欄裏走，跟在兩百格的備註欄裏走，一樣慢。根子不在攤開，在**光標進了排版的快
  取鑰匙**：只要摺疊開着，動一下光標就把整張表的排版丟掉重算（286 行：8 毫秒量隱藏、3
  毫秒算摺痕、3.4 毫秒排欄寬）。摺疊之前這個坑填過一次，摺疊落地時又踩了回去。
  現在**走過去不再攤開**——要讀全文開 `t i`（詳情欄會折行），要改字按 `i`，那一格纔攤開
  ——於是排版**與光標無關**，一份算好一直用，走一步只要 2.6 毫秒（同一張表在「基本」下
  是 1.9 毫秒，也就是說摺疊本身幾乎不要錢了）。攤開的那一格也不再把整欄撐寬：它從自己
  那道牆往右頂出去，別的行一格不動，牆和兩邊的空格照畫，邊界看得見。

- **`t1g` 跑到整份稿子的第一行去了。**（#283）「markdown 表格中按 t1g，会跑到整
  个文档的第一行而不是表格的第一行。」它拿的是**檔案**的行數去夾，於是一個表格的鍵能
  把光標帶到三屏以外的段落裏。現在夾在**這張表**裏：正文中數字仍是行號槽裏那個數（`t238g`
  就是第 238 行），小於表首或大於表尾就落在表的第一條／最後一條；全窗表格裏行號槽畫的是
  這張表自己的條號，`t1g` 就是第一條。

- **裝了 yume 的 Mac，`:yume-installed` 一個方案都找不到。**（#169）「我安装了 yume 并且有五个方案，但是 :yume-installed 没有办法检测到他们。」找碼表的那份清單只有
  Windows 一支和 XDG 一支，**macOS 掉進了 XDG 那一支**——而 `~/.local/share/yume/` 不是
  Mac 上任何東西的家。macOS 的 yume 是一個輸入法 App（`~/Library/Input Methods/Yume.app`
  或 `/Library/…`），編出來的碼表與使用者裝的方案在 `~/Library/Application Support/Yume/`。
  這五個地方按 yume 自己的順序加進去了（重編過的覆蓋目錄最前，App 內置的最後），bundle
  裏的 `Contents/Resources` 就當一個普通資料目錄遞進去——它本來就是 `data/` 加 `schemes/`
  的擺法。實測：從前 0 個方案、靈明不可用；現在 5 個方案、可用。

- **「畫成表格」（`t b`）現在你叫它摺它就摺。**（#283）2026-09-07：「虽然 tb 在默认
  状态下不折叠，但能不能在按下 tw 之后折叠？」能。「基本不藏、不摺、不替換」那條法管的是
  **那一級自己會不會動手**，不是「你能不能叫它動手」——`t w` 是你按的，而且它一個字都不
  往級別裏寫，鏈條沒斷。摺要有東西可以拿來量，而基本這一級和「完整」一樣把欄排齊了，所以
  量得了。真正沒得摺的只有**源碼**（照原樣畫，沒有一欄是排齊的），`t w` 在那裏改口說
  「先 tb 或 tf」，不再說「先 tf」。**你按過的那一次算數**：在基本裏攤開了，走到完整還是
  攤開的；沒按過就照那個地方的規矩——完整摺、基本不摺、全窗照它自己那個寬度。

- **全窗表格（`t t`）裏，長格子的後半截讀不到——一個字都讀不到。**（#283）2026-09-07：「tw 功能无法在 tt 模式下使用，导致所有的长单元格都是保持折叠状态且没有
  折叠符号，光标进入该单元格后不会自动展开……进入 insert 模式后，也无法导航到被折叠的
  部分。」三件事湊在一起就是「永遠讀不到」：全窗那張格子有它自己的 32 格上限，寫死在
  渲染器裏，`t w` 按下去只答一句拒絕；切斷的地方不畫記號，所以連「後面還有」都不
  知道；而光標站進去也不展開，Insert 也走不過去。現在三件都改了：**`t w` 在全窗裏跟
  在 `t f` 裏一樣算數**（全窗不是一個「級」，它是另一個問題，所以那句拒絕在這裏
  沒有意義），攤開之後每一欄畫到它自己那麼寬、最寬一個窗口；**切斷的地方立一個 `>`**，
  和正文裏摺起來用的是同一個記號；**光標所在的那一格拿窗口寬來量**，摺着也整格畫出來
  ——正文裏「站着的那一格永遠不摺」的同一條規矩，搬到格線上。

- **詳情欄（`t i`）把太長的欄位切在自己的邊上。**（#283）「信息面板也没有换行
  功能来显示这个单元格的全部信息。」摺起來的那一格，本來就是叫人來這裏讀全的——面板
  自己再切一次，那就哪裏都讀不到了。現在欄位值**折行**，用的是頁面自己那套折行規矩
  （禁則照樣管），續行縮在欄位值那一格起；面板捲動也改成按**畫出來的行**數，所以光標
  所在的那一欄連同它折出來的每一行都留在面板上，不會只露出第一行。

- **格線最右那一欄，尺子和名字比它的內容早停一欄。**（#283）欄號那一行和欄名那一行
  問的是「整欄放得下嗎」，內容那幾行問的是「這一欄的起點還在窗裏嗎」——兩個問題，於是
  最後那一欄常常**內容畫得好好的，頭上既沒有名字也沒有號**。從前很少撞見，32 格上限
  把大多數欄壓進了窗裏；`t w` 攤開之後它成了常態，而中招的正是讀者剛剛叫它攤開的那一
  欄。現在三行同一個停法。欄號仍舊右對齊到欄尾，欄尾出了窗就貼着窗邊畫——號碼落在窗
  外，等於沒有號碼。

- **摺疊記號 `>` 改成金的、加粗的，不再讀成你自己打的那個 `>`。**（#283）「折叠标志要不要加个下划线背景色什么的突出一下避免用户当作它是个普通的 `>`。」記號
  本身換不掉——Unicode 的省略號（`…`、`⋯`、`‥`）全是東亞**歧義寬度**，而表格正是編輯器
  的寬度和終端機的寬度必須逐格對上的地方：一個記號兩種量法，它後面每一欄都會錯一格。
  所以分別做在**墨色**上。從前正文頁上它是「批注」那一級的灰，格線上是**傢俱**那一級的
  灰（行號、格線、邊框），兩處都比它緊挨着的那行字還淡；整頁本來就是一串灰，灰的 `>` 就是你打的 `>`。現在兩處同用**金**加**粗**——金在這套配色裏的意思正是「這不是正文」。
  用墨色而不是底色：這一頁上的**底色是讀者的筆**（選中、朱批的高亮、光標那一條），
  摺疊記號是編輯器說的話，不該借讀者的筆來說。

- **小抄整條左邊框不見了，背後的字從那條縫裏長出來。**（#286）漢字佔兩格，
  終端機從第一格畫起；第二格讀回來是空的，可它**不是空位**，是別人的身體。面板
  的左邊界是拿自己的寬度算的（`視窗寬 − 面板寬`），跟漢字的邊界毫無關係，所以
  **大約一半的視窗寬度**下它正好落在某個字的右半格上——那一整欄的 `╭`、`│`、`╰`
  程式都寫了，可是渲染器**跳過寬字蓋住的格子**，一個都送不出去。`Clear` 救不了，
  肇事的那個字起點在面板**外面**。修法是定下的，也最直接：**把闖禍的那個漢字整個
  抹成空白**，那一格就空出來給邊框。漢字沒法只遮一半——要麼整個在，要麼整個不在。
  按 `t` 的表格小抄、空白鍵主選單、`:` 命令選單、查找選單、檔案挑選器、側欄，
  六處走同一支 `clear_wide_left_edge`；HUD 那份「往左讓一格」的特例（#284）
  **刪掉了**，它其實也是把那個字整個蓋掉，只是順手挪了面板。70 到 110 格逐格驗過。

- **`t f` 摺起來的長格，後面還拖着一大片空白。**（#283）摺的是格子裏的**字**，
  而欄寬量的是**兩根豎槓之間**——已經在檔案裏排整齊的表（`:table-rules` 排過的、本專案
  文檔裏的每一張），每一格都拿空格填到了整欄那麼寬，於是短格照樣投 43 格的票：`>`
  停在字斷掉的地方，後面十四格空白一直排到豎槓，摺了等於沒摺。現在檔案自己那份空格
  **跟着尾巴一起下頁**（走 `hidden_on_line` 那一條，寬度、折行、鼠標讀的是同一張單
  子），`>` 之後只剩一到兩格。兩條界限：**只讓到上限爲止**——放得下的表照舊一格不動，
  對齊也原樣；還有，光標**站在哪段空白上，那段就露出來**，不是整格露出來（整格的空格
  有一欄那麼寬，走一步 `l` 整欄就會鼓一下）。豎線那一行的橫槓也算空白（那是畫，不是
  字，本來就會重畫），留一根，冒號一個不動。

- **竪排根本沒有那個牌子。**（#284）打了一半的命令在光標旁邊那一份，橫排上有，
  **竪排上一次都沒出現過**——它找空白的辦法是「本行最後一個有字的格子之後」，那只有
  在從左往右寫的頁面上纔是空白處；竪排從右往左填，於是只要那一橫上還有任何一個字，
  這個位置就頂到右邊緣，所有候選都放不下。現在找的是**連續的空白段**，兩個方向同一個
  答案，順帶還能找到兩縱短句之間的那個縫。角也跟着改：空白在光標左邊的時候，角掛在牌
  子的右端指回來——指向反方向的角比沒有角更糟。

- **`t w` 從來沒有出現在任何一張 `t` 的菜單上。** 摺格子按得動、手冊上寫着、
  在哪裏都能按，可按下 `t` 之後彈出來的那張「後面能接什麼」的單子，四種情形
  一種都沒列它。現在它排在 `t b`／`t f` 旁邊——它自己的源碼註釋早就說了該在那裏。

- **手冊裏還有十處寫着 `t n` 和 `t a`。** 表格操作是 `t b`、畫成表格是 `t f`，
  2026-09-06 就定了，同一頁下面那張鍵位表也早已改對——偏偏章首那張「四種看法」的表
  沒動，底下四段正文也沒動，而那正是讀者第一眼看的地方。

- **`:word-show` 的兩種畫法，菜單上終於有了。** 分詞著色一直有底色和字色兩種
  （`:word-show tint`／`ink`，中文寫法 `底色`／`字色`），跑得動、手冊上寫着，
  可命令表裏 `show` 後面只宣告了 `on｜off`——**菜單問「後面能接什麼」，答的是兩個，
  實際收四個**。一個能用而沒人找得到的詞，等於只有寫它的人自己有。

- **配置檔裏 `[theme] name = "黑白"` 從前只換了名字。** 主題名認 `bw`、`heibai`，
  **不認漢字**，於是那一行落進「認不得就當自定義主題名」那一支：顏色還是墨香，
  頁面卻自稱黑白。十套主題的漢字名現在都認（`墨香`、`黑白`、`藍曬`、`琥珀`、
  `莫高`、`莫蘭迪`、`夜螢`、`明度階`、`陶窯`、`靛橘`，簡體同認）。
  命令行那一頭照舊只認拉丁字母——`:` 這一行本來就把輸入法擋在外面。

- **手冊裏的主題名跟菜單對齊了。** 手冊教的是 `:theme moxiang`／`heibai`，
  而 `:theme` 按 ⇥ 列出來的是 `ink`／`bw`——同一套墨兩個拼法，其中一個菜單裏
  找不到。手冊改成菜單認的那一個，拼音仍舊收。

- **`:` 的菜單認得它自己印出來的簡寫了**。`:table` 的菜單把每個詞的最短寫法
  印在括號裏——`off (of)`、`rules (r)`——命令名自己也一樣，`:tab` 就是 `:table`。
  可是打 `:tab` 再按空格，菜單整個空掉：**跑得動的寫法，菜單不認**。（`:tab rules`
  一直是能跑的。）現在菜單走的是解析器那一支：命令名、子命令、`!`，同一條規矩，
  同一個地方寫着。

- **`:view-hanging off` 從前是把標點旁置打開**。菜單上寫着 `on｜off`、提示行也
  印着，可解析那一頭根本沒讀那個詞——`:view-hanging` 不論後面跟什麼都只是「反過來」，
  所以連按兩次 `:view-hanging off` 纔看得出來：第一次開，第二次關。`:yume-chaifen off`
  是同一個形狀。現在十一條給「開｜關」的命令走同一支讀法，菜單印的簡寫（`of`）
  也一樣認得；光寫 `:view-hanging`、`:view-dense`、`:readonly` 這種不帶詞的，意思照舊不變。

- **選檔案的那個框打得了中文了**。`空格 f` 找檔案、`空格 b` 換文件，開出來的
  那張清單從前只認得鍵盤直接打得出來的字母——中文稿子的檔名是《第三章》，能打的
  只有 `.md` 那三個字。現在它跟 `/` 搜索一樣：輸入法在裏面照常跑，候選面板就掛在
  查詢那一行下面。（篩選那一段本來就會算中文，是字根本沒送進去。）

- **鎖住的稿子，markdown 表格鍵不再謊報成功**。`:readonly on` 之後在表格裏按
  `t r`，狀態欄說「加了一行」而表格一動不動——文字本來就是安全的（鎖住的稿子誰也
  搬不動），丟的只是實話。同一個 `t` 選單的 `.csv` 那一半一直是好好拒絕的，`.md`
  這一半不是。現在加行、刪行、加欄、刪欄、挪行、挪欄、對齊、排序、貼表，九件事
  走的是同一扇門，那扇門會說「只讀」。

- **`:rec!` 說沒這個命令**。`:recover` 有，`:recover!` 有，`:rec` 有，偏偏
  `:rec!` `:recov!` `:recove!` 都說不認得——縮寫和驚嘆號各自都對，湊到一起就不對。
  （認得驚嘆號的那張表和寫出驚嘆號的那張表是兩張，`recover` 只進了一張。現在是
  一張。）

- **`:export` 問錯了東西**。它的提示寫着 `<檔名>`，而它真正先讀的是格式，於是
  照提示打 `:export 第三章.md` 得到的是「沒有『第三章.md』這種格式」。現在提示按
  順序來：先 `html`／`typst`／`csv`／`tsv`，再檔名。順帶，那四個格式名從前是整個
  編輯器裏**唯一**在 `::` 裏搜不到的四個詞——你得先知道它們才找得到它們。

- **`:help chinese` 教了一條會報錯的命令**。它寫着 `:segment on`，而分詞早就併進
  `:word` 了，照着打得到「沒有『segment』這個命令」。改成 `:word-show on`；README
  裏那一句也一樣。

- **一份文件裏第二張表的寬度**（#283）。編輯器記的是你**進去的那一張**表有幾欄，走到
  另一張寬窄不同的表上，凡是要數欄的都還在數上一張：`o` 開出來的新行寬度不對；`3gd`
  被夾到窄表的兩欄，去第二欄找，**還把第二欄的名字寫進答案裏**——錯得有名有姓，最難
  不信；`:!sort` 這類把整段交給外部命令再收回來的，每一行都被判「寬度不對」拒收；
  `:table-check` 更是把整份文件從頭讀到尾，每一段散文都算一行寬度不對。現在這四處問
  的都是光標腳下這張表，`:table-check` 只查你站着的那一張，分隔行（`|---|`）既不查也
  不算進行數。

- **詳情欄擺錯了邊，於是只露得出兩格**（#283）。面板是掛右邊還是鋪底下，從前看的是
  「表格有沒有佔滿窗口」——所以稿子裏的表格拿到的是**腳注**那條四行高的底欄：五欄的
  行只看得見兩欄，拆分表二十八欄的行也只看得見兩欄，而且頁面上沒有任何地方說還有下
  文。現在看的是**面板裏裝的是什麼**：註是一段短話，鋪底下對；一行表格是個高東西，
  寫在哪裏都是高東西，掛右邊。

- **用空格或 Tab 隔開的表格，詳情欄是空的**（#283）。那種表格（碼表、`dict.yaml` 那
  一類）的欄是猜出來的，而面板去拿欄名時把它的第一行當 markdown 表頭、照 `|` 切——切
  出一欄，於是什麼都列不出來。只有 `|` 表格纔需要回頭讀表頭。

- **系統輸入法在 Insert 模式下打不了字**（#271）。上屏出來的全是空格——那個空格是上屏
  鍵本身。啟動時開的 kitty 鍵盤協議裏有一項 `REPORT_ALL_KEYS_AS_ESCAPE_CODES`：開了
  之後文字鍵不再當文字送，改成 `CSI 鍵;修飾;文字 u`，而**文字在第三個參數裏**，
  crossterm 0.28 解析完就丟掉。ASCII 不痛不癢（鍵就是文字），系統輸入法上屏一句中文
  就只剩那一下空格。那一項撤掉了；⇧ 單擊切換中英要的另外三項照舊。

- **`t` 的提示面板列的是搬走了的鍵，而且從不列進出的路**（#272）。面板上有 `s` `S`
  `o` `O` 這些「整個檔案是一張表」纔有的鍵，而 markdown 表底下按了沒反應；`t t`
  `t q`（進表格／出表格）和 `t f`（對齊）一條都不在上面。現在按你站在哪種表分開列，
  進出的那兩個鍵永遠在第一行。

- **裝了卻沒用上的數據檔會說話了**（#220）。`load_data_file` 從前只答一個 `bool`，
  於是「檔案不在」和「檔案在，而核心不收」是同一個答案——而核心其實一直有理由可給。
  `.ydiv` 的魔數 2026-09-04 從 `YDV20260828` 換成 `YDV20260904`，舊的數據目錄從那天
  起**一聲不響地**丟掉全部拆分注解：檔案都在，注解沒了，哪裏都不說為什麼。現在
  `:yume` 會把話說完：「`data/chaifen.ydiv` 核心不認：bad division magic（期望
  YDV20260904，檔頭是 YDV20260828）」。**沒裝的檔不算**——一半的清單本來就是可選的，
  真列出來反而把要緊的那一行埋了。

  順帶兩件：**同一個檔不再裝兩遍**（出廠清單的兩半在讀音表上是重疊的，那是 13 MB
  解析兩次換來同一張表），以及**字根表壞了不再連累拆分表**——一條清單項是兩個檔，
  第二個沒讀成，第一個照樣進引擎：拆分・讀音・字集都在它上面，只有編碼在字根表上。

- **`:view-wrap off` 之下頁面會跟着光標往右走了**（#221）。從前不折行的長段落只畫得出
  螢幕那麼寬的開頭一截：`gl` 走到行尾，光標出了右邊界，它落在的那些字**一次都沒有
  畫出來過**。現在窗口跟着光標橫向移動——一次只挪它差的那一格，不是整屏跳，所以沿
  着一行長句一個字一個字走的時候頁面不會從眼底下抽走。行號欄不動（它是家具不是正
  文），被切在中間的漢字畫成空白而不是半個字，滑鼠點擊也照同一個數算。豎排與表格
  本來就是兩軸的，不受影響。

- **`:view-wrap off` 在竪排下會說「這一項只管橫排」了。** 一縱斷在窗高上，開關折行竪排根本
  沒有東西可關；從前它照收不誤，回一句「長段落跑出右邊」——竪排沒有那條右邊。
  （`:view-wrap 40` 兩邊都算數：橫排是行寬，竪排是一縱四十字，那個沒變。）

- **`t f` 對齊寬表格。** 本來有一道 32 格的上限：比它寬的格子照樣整格寫出來，
  但**別的行就不再補到它那麼寬**，於是那一欄的右豎線各自為政——手冊裏那張表
  第二欄有 60／76／100／115／134 格五種寬度，右邊看着就是一排參差的 `|`。
  上限去掉了：一欄補到自己最寬那一格為止，豎線永遠齊，代價是寬表格會超出頁面，
  往右捲就是。
  （算的仍舊是**原始碼**的寬度，所以 GitHub、別的編輯器、`less` 看見的都是齊的；
  所見即所得把 `` ` `` 和 `**` 藏起來之後螢幕上還差幾格，那一半歸 #212。）

- **分詞的底色和 `==高亮==` 分不出來**（#208）——兩個都是朱，一個洗到 78%，
  一個 91%，相差 1.23:1，等於沒有分別。現在詞的底色是階梯上一格中性的灰
  （WORD 962），高亮是金、洗到一個對比度目標為止。階梯安靜的那一頭也跟着重排：
  SELECTION 700、HEAD 815，是搜出來的——莫蘭迪（對比最低的那一套）選中之後
  正文仍然有 4.5:1。
- **HUD 挑有地方的那一行**（#207）——光標在最後一行時它本來無處可去；
  現在先看下面一行，沒有就看上面一行，和命令面板一樣。

- **沒裝宇浩資料的機器上，`w` `b` `e` 在繁體上一個字一個字挪**——**編進二進制的
  那份**後備詞表只有簡體，抬頭、已經、時候、什麼都不在裏面。裝了資料的機器用的是
  宇浩自己的語言模型（`lang.ywl`，繁簡混合，一百多萬條），它一直都認繁體——
  現在有一個對着真資料跑的測試把這件事釘住。後備詞表也補齊了兩種字，
  外加小說真正在用的敘述語（說道、笑道、卻說、只見、姑娘、丫頭……，
  按九部語料的實際詞頻換算）。
- **`)` `}` 會多吃下一句的第一個字**——往前走的動作要停在下一個東西**前面**一個字，
  `w` 一直是這樣，這兩個不是，所以 `)d` 悄悄改壞了下一句。
- **`1G` 去了最後一行**；**`3gd` 收下了那個 3 然後不用**。
- **按字走的時候 `t` 按不出來**——課文正是在那裏叫你按 `t/`；
  **`t S` 把分隔檔正着排**，跟按鍵反着來；`t` 的選單和提示補上了 `/ ? g s S`。
- **什麼都沒做就什麼都不說**：暫存器空的 `p`、`R`、沒錄過的 `Q`。
  退場的 `Enter` 和 `*` 也會說現在該按哪個——按它的人不是從 vi 來的，是從上週來的。
- **`C-Space` 根本沒接**。課文第一件事就是叫你按它，`:help` 也寫着，
  而它一路掉到編輯器裏，沒有人綁過。
- **純文字的小說沒有大綱**——資治通鑑七百卷，一個 `#` 都沒有。
  現在「第四百一十二卷」這種行也算標題（只在找不到 markup 標題時才找），
  `:toc` 開的是大綱欄，不是把七百條擠進狀態列。
- **排版**：長數字不再兩個兩個湊——「2026」變成「20」「26」是兩個數字，
  現在一格一個字往下排；同一行的第二個注音不再被丟掉；
  詞的注音**居中**對着詞（JLREQ §3.3.6），橫排竪排都是。
- **一個字幾格，兩處說法不一**：`yumete-cjk` 預設是寬，`main.rs` 的 auto 落到窄。
- **預覽伺服器**：不是 tinymist 的認不出來（現在記下程序名）；
  pid 檔不再跟着 symlink 走；Markdown 的 `:view-preview` 第二次不再失敗。
- **`--shot`**：把一幀畫成文字印出來就退出。`:shot` 要窗口、要桌面、要人看；
  這個在沒有窗口的機器上也能用——寫報告、做審查、留一張「當時長這樣」。

### 修好的 · Fixed

- **排序之後表格不再變回原文。** `t1s` 排完，畫面直接回到源碼視圖——排序改的是
  **文字**，而它從前是把整份文件都忘掉重問一次「這是不是表格」，一份旁邊沒有 schema
  的 `.csv` 於是丟掉了讀者親手給的答案。現在只忘掉跟文字有關的那幾份快取。

- **滾輪一格走多遠可以自己定了**（#222）。`[editor] wheel_step`（出廠 3），或者隨時
  `:wheel 1`；`:wheel` 單獨用是報現在是幾。橫排數行，竪排數縱。

- **鼠標快速滾動不再把終端卡死**（#268）。從前一個滾輪刻度畫一幀，而觸控板一次滑動
  送幾百個刻度：拿資治通鑑量過，3000 個刻度是 3000 幀、**59.5 MB** 的轉義序列。編輯器
  每一幀都答得很快，慢的是終端——從讀者那一側看就是整個卡住，只能關掉分頁。現在一次
  滑動只讀一次、只畫一幀，同樣 3000 個刻度是 **1.1 MB**。中途按下的鍵不會被吞掉。

- **打字提示條就近顯示**（#269）。從前的規矩是「下一行，放不下就上一行，第一個放得下
  的就是它」——它只問「有沒有位置」，不問「那個位置有多遠」。於是空行上它跑到下一行的
  最右端，離光標半個屏幕；句子長了反而回到光標旁邊。現在是**就近**：光標這一行優先，
  然後下一行、上一行、下兩行、上兩行，按離光標多遠挑。角上那個符號跟着落點走
  （`─` 同一行、`╰` 在下、`╭` 在上）。它照舊不會蓋住任何字。

- **表格裏點擊落錯格**（#196）——點擊解析只有「散文」和「竪排」兩個分支，
  沒有表格分支，於是偏掉左邊每一欄補的空白，看起來像亂跳。
- **一個字幾格，問終端**（#200）——`ambiguous_width = "auto"`：開機時印一個 `—`
  出去，問光標停在第幾格。算錯的後果不是難看：一行有十個 `——`，光標就飄十格。
- **`j`／`k` 用的是畫出來的欄位**——所見即所得開着時，每有一個 `**` 就偏一格；
  關掉折行還會停在一個根本沒畫的星號上。
- **禁則處理問「讀者看到的字」**，橫排竪排都是；一次退一個**看得見**的字。
- **跳轉一律落在中間**（#182）——往前找和往後找落點一樣。
- **`Esc` 關掉的是預覽窗，不是這次搜索**（#201）——關了以後 `n` 走下一處，窗自己回來。
- **寫入按身份認，不按拼法認**——`:export` 不會因為路徑寫法不同而蓋掉正在編輯的稿子；
  `:w <路徑>` 是抄一份（`:write-as` 才搬家）；只讀檔案不會被靜默覆蓋。
- **表格的格數不變**這件事不再問 `:table` 開了沒——`gJ`、`:replace`、`:s`、
  `:ruby-format`、注音寫入都認。
- **看表格不再重排它**——進表格模式不動檔案，`t t` 才是「對齊一下」。
- **`.` 重複的是剛才那一次改動**，不是更早的某一次。
- 竪排一段一幀只排一次（50 萬字單段的一幀從 16.9 秒到 17 微秒）；
  `block_of` 從 6.9 µs 降到 9 ns。

---

## 0.1.0 之前 · Before 0.1.0

`docs/development.md` §5.2 是完整的功能表，§「What four reviews of the code
found」以下記着每一輪審查發現了什麼、怎麼修的。
